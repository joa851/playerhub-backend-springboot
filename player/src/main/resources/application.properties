spring.application.name=player
server.port=${PORT:8081}

# Config Server: trae toda la config no-secreta (datasource URL, hibernate,
# pool sizes, URLs de servicios externos, base-url de LLM, etc.).
spring.config.import=optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}

# Eureka: necesarios en arranque, antes de poder hablar con Config Server.
eureka.client.enabled=${EUREKA_CLIENT_ENABLED:false}
eureka.client.serviceUrl.defaultZone=${EUREKA_DEFAULT_ZONE:http://localhost:8761/eureka}

# Secrets: vienen de Secret Manager en producción, defaults en local.
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:admin123}
api.football.key=${API_FOOTBALL_KEY:}
llm.key=${LLM_KEY:}
